# Worklog

---
Task ID: 1
Agent: Super Z (main agent)
Task: Build the complete Arabic RTL, mobile-first, privacy-first personal recovery & self-control web application per the user's uploaded master prompt (Pasted Content_1789729011462.txt — 76 specification sections).

Work Log:
- Read and analyzed the full 2,539-line master prompt; classified the task as Type 3 (interactive web development) and loaded the fullstack-dev skill.
- Initialized the Next.js 16 + TypeScript + Tailwind 4 + shadcn/ui environment (init-fullstack.sh).
- Foundation: `src/app/layout.tsx` with `<html lang="ar" dir="rtl">`, IBM Plex Sans Arabic via next/font (self-hosted, no runtime external requests), next-themes (dark default); `globals.css` with calm teal design tokens (teal primary, green success, orange warning, red reserved for risk/emergency), RTL-safe utilities, tabular numerals, safe-area insets, reduced-motion support.
- Types & taxonomy (`src/lib/app/types.ts`, `src/data/app/taxonomy.ts`): 10 risk levels with labels/descriptions/examples, 5 trigger categories + 24 triggers, 10 early warning signs, 8 anti-rationalization patterns with firm responses, 7 journey stages + honest disclaimer, boredom menus (5/15/30 min), Work-Safe protocol steps, 6 digital protection guides with honest limits, support person neutral message templates, spiritual practices (gated), when-to-seek-help list.
- Intervention library (`src/data/app/interventions.ts`): 28 structured interventions (cut-chain / change-state / prevent-return / support) each with name, triggers, risk range, duration, location, instructions, whyItHelps, nextAction, workSafe/alone flags.
- Knowledge bank (`src/data/app/knowledge-*.ts` + index): 98 items across 20 categories, each with اعرف/افهم/افعل/تذكّر + optional deep reading, stage hints, trigger tags, spiritual gating; evidence-informed with appropriate hedging (no sensational claims, no invented citations).
- Engines (`src/lib/app/`): risk-engine (3-dimension weighted model with behavioral floors: browsing-started ≥6, proximity ≥8 → ≥7, crisis ≥9; context bonuses capped; mode mapping 1-3 awareness / 4-5 interrupt / 6 immediate / 7-8 emergency / 9-10 maximum), intervention-engine (scoring: risk fit, trigger match, environment, work-safe, personal success history, recency avoidance, escalation set), dose-engine (current needs over journey day: recent relapse → relapse/self-compassion, elevated risk → urges/emergency skills, frequent triggers → matching tags, stage + preference nudge, 10-day non-repetition, date-seeded stable pick), progress (7+ meaningful indicators, insights, forecast from evening check-in — no fake recovery %).
- State (`src/lib/app/store.ts`): zustand + persist to localStorage key `istiaada-state-v1` with partialize (data only), versioning, export/import JSON (validated, confirm semantics), confirmed reset, 4 default If–Then rules, relapse add/review/delete, check-ins, plans, dose log.
- UI: shared components (NumberScale 1-10, ChipMultiSelect, CheckItem, StatTile, InfoNote, StepDots, ToggleRow), Countdown timer (ring + compact variants), InterventionCard with full structured data.
- Shell (`AppShell.tsx`): mobile bottom nav (Home / Dose / center persistent red Emergency / Plan / More sheet with all 12 sections), desktop start-side sidebar, framer-motion screen transitions, emergency pulse animation.
- Onboarding: 8 steps per spec (goals, difficult times, pre-behavior patterns, device needs, build goals, support preference, personal Why + reason chips, initial plan summary).
- Screens (12): Home (state-aware: normal / moderate / high-risk / post-relapse + dose card + plan pillars + progress snapshot), Urge Check (3 dimensions + context toggles + triggers → risk result with mode-appropriate responses + anti-rationalization panel + intervention + outcome loop), Emergency Mode (full-screen takeover, 4 steps: cut chain → change context → one intervention with timer → reassess; Work-Safe variant; escalation toward people/trusted person; maximum-risk ultra-minimal UI), Relapse Center (stop-second-fall 6 steps → quick log → reframe messages → calm review wizard 6 questions → auto-suggested If–Then rule from intervention point), Daily Plan (minimum/standard/extra modes, 8 sections with checkable items + editable purpose task + body choices, evening check-in dialog with 5 questions + 4 condition scales → next-day forecast banner), Daily Dose (full اعرف/افهم/افعل/تذكّر + done/skip + recent doses + stage info), Trigger Map (honest empty state <3 events; top pattern + first warning sign + best cut point + time-of-day distribution + trigger frequencies), Prevention (If–Then CRUD + toggles, digital protection guides with honesty note, support person with neutral templates + tel: link), Progress (journey stepper + 10 indicator tiles + insights + honest disclaimers; streak shown as one small metric), Values & Spiritual (Personal Why editor with highlight card, values draft, spiritual section gated by toggle), Knowledge Base (search + 20 category filters + card dialog with deep reading), Settings (privacy explanation, journey date, preferences incl. notifications with neutral wording, theme dark/light sync, export download/copy, import with validation, AlertDialog-confirmed reset, when-to-seek-help).
- Fixed during verification: TIME_BUCKET_LABELS wrong-module imports (2 files), React Compiler lint errors (setState-in-effect → useSyncExternalStore hydration guard; preserve-manual-memoization → direct pure computation), duplicated "إذا" prefix in If–Then rule texts, emergency completion now closes originating pending urge check, stale render data in outcome marking.
- End-to-end browser verification (agent-browser + VLM visual QA): full onboarding walkthrough → Home; high-risk urge check (9/10 → maximum intervention → single CTA); complete Emergency flow (steps 1→4 → completion "هبط الخطر — أحسنت"); full relapse flow (stop steps → quick log → reframe → calm review wizard → auto-added suggested rule verified in localStorage); low-risk check (2/10 → awareness mode); Work-Safe intervention correctly selected when device needed; evening check-in saved → forecast banner shows "اليوم قد يحتاج إلى استعداد إضافي"; dose engine prioritized relapse content after recent relapse; Progress metrics computed from real session data; Trigger Map honest empty state; spiritual content gated off/on; export downloaded as valid readable JSON; theme toggle verified via html class; localStorage persistence across reload verified; VLM QA: Arabic typography/RTL excellent, layout strong, contrast very good. Final: ESLint clean, 0 console errors, 0 page errors, dev server GET / 200.

Stage Summary:
- Delivered a complete, browser-verified Arabic RTL recovery web app ("استعادة") implementing all core systems of the master prompt: 3-dimensional risk model, risk→intervention mapping, Emergency Mode with Work-Safe variant, anti-rationalization engine, relapse stop-second-fall + calm review + auto-generated prevention rules, daily dose adaptive engine (98 knowledge items), trigger map, progress with meaningful multi-indicators (no fake %), local-first privacy (localStorage only, no accounts/servers/API calls for user data), export/import/reset with confirmation, no-streak-culture, no-shame/no-rationalization language throughout.
- Key files: src/app/{layout,page,globals.css}; src/lib/app/{types,helpers,risk-engine,intervention-engine,dose-engine,progress,store}.ts; src/data/app/{taxonomy,interventions,knowledge-*}.ts; src/components/app/{AppRoot,AppShell,Onboarding,Timer,InterventionCard,shared}.tsx; src/components/app/screens/*.tsx (12 screens).
- All state remains on-device (localStorage `istaiada-state-v1`); the only route is `/`; no backend, no database, no external data flow.

---
Task ID: 2
Agent: Super Z (main agent)
Task: Final engineering + UX audit of Masar V2.1.1 (استعادة) per user's 14-section audit brief.

Work Log:
- Read all core files: AppRoot, store, Onboarding, AppShell, risk-engine, Settings/Emergency/Urge/Relapse screens, shared components, dialog/sheet/alert-dialog primitives, helpers, types, taxonomy.
- Browser audit at 390x844 and 360x640: completed full onboarding (Case A verified: onboardingCompleted persists, reload skips onboarding), walked all 12 screens (zero horizontal overflow), bottom nav clearance 45px OK, emergency flow E2E (opens, steps advance, intervention logged success:true, completion screen, return to app), reset AlertDialog works, export produces valid JSON envelope {app, exportedAt, data}.
- Ran tsc (2 real errors: arabicDate(new Date()) in DoseScreen:44 + PlanScreen:131) and eslint (clean).

Findings (to fix):
1. CRITICAL import validation too weak (only app id + userProfile existence) — no version/schema/type checks, no atomicity guarantee.
2. CRITICAL onboarding has no restore-from-backup path (Case C missing).
3. HIGH More sheet clips on small phones (759px content vs 640px viewport, overflow: visible — Settings/Emergency cards unreachable).
4. HIGH DialogContent + AlertDialogContent primitives lack max-height/scroll — long dialogs overflow mobile viewports.
5. HIGH Settings import lacks explicit confirmation gate when existing data present (required wording: "استعادة النسخة الاحتياطية ستستبدل بياناتك الحالية. هل تريد المتابعة؟").
6. MEDIUM unused backend scaffolding: src/app/api/route.ts, src/lib/db.ts (Prisma), prisma/, db/, .env DATABASE_URL, db:* scripts — contradicts local-first claim.
7. MEDIUM risk engine: level 10 has no explicit behavioral definition (only reachable via near-max weighted math).
8. LOW: version footer says ١٫٠ (should be ٢٫١٫١); Radix a11y warnings (missing Description on 2 dialogs); riskColorVar duplicate branch; onboarding min-h-screen → dvh + sticky nav for keyboard; backup envelope lacks schemaVersion/appVersion.

Stage Summary:
- App is fundamentally solid (persistence, emergency, RTL all verified working). Fixes needed: robust backup validation + onboarding restore path + mobile dialog/sheet overflow + scaffolding removal + risk engine level-10 definition + minor cleanups.

---
Task ID: 3
Agent: Super Z (main agent)
Task: Implement all audit fixes for Masar V2.1.1 (استعادة) — backup/restore safety, onboarding persistence paths, mobile UX, privacy scaffolding removal, risk engine determinism, code cleanup.

Work Log:
- NEW src/lib/app/backup.ts: full backup validation module (APP_ID, ACCEPTED_APP_IDS, BACKUP_SCHEMA_VERSION=1, APP_VERSION=2.1.1). validateBackup() checks: JSON parse → envelope app id → schemaVersion (explicit or legacy data.version fallback) → every AppData field type/enum/range (userProfile, journey, urgeChecks items incl. 1-10 ranges + context booleans, interventionLogs, relapseEvents, preventionRules, checkIns, plans, doseLog, supportPerson, settings) → returns clean known-shape AppData (strips unknown fields) + Arabic error messages. buildBackupJson() emits envelope {app, schemaVersion, appVersion, exportedAt, data}.
- store.ts: importData() now ATOMIC (validates fully via validateBackup BEFORE set; failure leaves state untouched); exportData() uses buildBackupJson (schemaVersion + appVersion added).
- NEW src/components/app/RestoreBackup.tsx: shared restore panel — file picker (20MB cap) + paste fallback, live validation preview (summary card: export date, counts), explicit confirmation AlertDialog when existing data ("استعادة النسخة الاحتياطية ستستبدل بياناتك الحالية. هل تريد المتابعة؟"), success/error banners, disabled restore until valid.
- Onboarding.tsx: added welcome step (step -1) with two clear paths — "ابدأ كمستخدم جديد" + "لست مستخدمًا جديدًا؟ استعد بياناتك" (RestoreBackup, no confirm gate on fresh install). min-h-screen → min-h-dvh + safe-area bottom; wizard nav now sticky bottom-0 (keyboard-safe on mobile); StepDots hidden on welcome.
- SettingsScreen.tsx: import dialog rebuilt around RestoreBackup with hasUserData() detection; trigger renamed "استيراد / استعادة"; version footer → ٢٫١٫١ via APP_VERSION.
- REMOVED backend scaffolding (privacy/local-first): src/app/api/route.ts, src/lib/db.ts, prisma/, db/, .env (DATABASE_URL), db:* scripts, prisma+@prisma/client deps (bun install re-synced lockfile). package.json → name "masar", version 2.1.1. Only route left: /.
- risk-engine.ts: documented deterministic rule ladder; level 10 now explicitly defined as proximity ≥9 AND control ≥9 (at the point of acting + near-total loss of control) — not reachable via urge alone; riskColorVar duplicate branch removed. taxonomy.ts level 9/10 descriptions updated to match definitions.
- Mobile: dialog.tsx + alert-dialog.tsx primitives get max-h-[calc(100dvh-2rem)] overflow-y-auto; sheet.tsx bottom variant gets max-h-[92dvh] overflow-y-auto; AppShell More sheet gets safe-area bottom padding + aria-describedby={undefined}; removed redundant usage-level max-h classes (Knowledge/Plan dialogs).
- FIXED horizontal overflow bug found on Values screen (360px): raw inputs in grid-cols-[110px_1fr] refused to shrink (min-width:auto) → 105px leftward RTL overflow → grid now stacks on mobile (grid-cols-1 sm:grid-cols-[110px_1fr]) + min-w-0 on inputs.
- a11y: aria-describedby={undefined} added to all DialogContent usages + More sheet (eliminates all Radix "Missing Description" console warnings); arabicDate() accepts Date|string (fixed 2 TS errors in Dose/Plan screens).

Verification (all passed):
- Unit: scripts/test-backup.ts 19/19 (new+legacy formats, invalid JSON, wrong app id, future schema, type violations, range violations, polluted fields stripped, round-trips); scripts/test-risk.ts 15/15 (floors, level-10 definition, determinism, mode mapping).
- tsc --noEmit clean (src), eslint clean, next build clean, standalone prod server 200 + RTL page.
- Browser E2E: new user (welcome → wizard → app → reload skips onboarding); returning user; fresh restore via file picker → atomic full-state restore (checks/rules/theme/spiritual/journey/support) → NO onboarding; invalid backup → clear Arabic error + button disabled + state untouched; garbage JSON → parse error message; existing user import → confirm gate → cancel leaves data intact → confirm replaces state; legacy-format backup imports correctly; new export envelope {app, schemaVersion, appVersion, exportedAt}.
- Mobile E2E at 360x640 and 320x568: zero horizontal overflow on all 11 screens; More sheet now scrolls (was clipping 120px on small phones); import dialog fits + scrolls; onboarding sticky nav visible with textarea focus; emergency level-10 flow (single CTA → maximum UI); low-risk awareness flow; desktop 1024 clean.
- Console: 0 warnings, 0 errors after exercising sheet + all dialogs.

Stage Summary:
- App is now: onboarding only for genuinely new users (with restore path), atomic validated backup/restore with confirmation gate, fully mobile-safe (dialogs/sheets scroll, no overflow at 320px+), backend-free local-first privacy preserved, deterministic documented risk ladder with explicit level-10 behavioral definition, version 2.1.1. Test backups for future QA live in upload/ (test-backup-valid/invalid/garbage/legacy/minimal.json).

---
Task ID: 4
Agent: Super Z (main agent)
Task: FINAL PRODUCT + UX + BEHAVIORAL AUDIT of استعادة (Istiaada) recovery app — read-only, no code changes, report findings grouped by priority.

Work Log:
- Read ALL app code: 12 screens, AppShell/AppRoot/Onboarding/RestoreBackup/InterventionCard/shared, store/risk-engine/progress/dose-engine/backup/helpers, taxonomy + knowledge-core content sample.
- Live browser audit at 390x844 and 320x568: full onboarding walkthrough, Home in all 4 states (normal/high/post-relapse/after-review), Urge Check input → level-10 result → Emergency Mode full flow → completion → return landing, Relapse full journey (stop steps → quick log → reframe → review wizard → suggested rule added), Settings + import dialog + valid backup preview + confirm gate + invalid JSON error path, More sheet.
- Measured geometry via bounding boxes: urge compute CTA at y=1381 on 844px viewport; relapse STOP CTA at y=707 on 568px viewport (below fold); NumberScale buttons 20px wide at 320px; bottom nav top=777 at 390px.
- Verified store state mid-flow: after Emergency success, urge check outcome="handled" but UrgeScreen phase stuck on level-10 result CTA, and Home still shows red high-risk banner + تدخل الآن (currentHomeState ignores outcome).
- Grepped settings usage: postRelapseSupport never read by UI (dead toggle); notificationsEnabled has no scheduling logic (placebo); preferredDoseTime dead.
- SOS call sites: AppShell bottom-nav center, HomeScreen normal card, HomeScreen high button all hardcode workSafe:false — ignore deviceNeeds profile promise.
- VLM visual QA on 9 key screenshots (home states, urge result 10/10, emergency, relapse stop, 320px screens) — confirmed CTA-below-fold, crowding at 320, competing alerts post-relapse; praised emergency screen as ideal panic UI.
- Console: 1 reproducible Radix warning from RestoreBackup confirm AlertDialog (custom id overrides auto-association); 0 page errors; no horizontal overflow anywhere at 320/360/390.
- NO code was modified in this task (audit only).

Stage Summary:
- Findings: 3 critical (stale post-resolution urge result screen; Home high-risk banner persists 3h after handled outcome; four-situation mental model not present in daily UI — primary goal gap), ~10 important (urge check form length under stress, touch targets 20px, STOP CTA below fold, SOS ignores Work-Safe profile, placebo settings, session-only values draft, missing self-report disclaimer on result screen, emergency terminology drift, Home SOS visual weight in stable state, interrupt-mode copy catastrophizing at level 4), plus polish items. Full report delivered to user in chat; awaiting approval before implementing any fixes.

---
Task ID: 5
Agent: Super Z (main agent)
Task: Implement the approved FINAL PRODUCT + UX + BEHAVIORAL AUDIT fixes for استعادة (Masar) — all 3 critical, all 10 important, 6 selected polish items; smallest practical changes only, no redesign.

Work Log:
- NEW src/lib/app/emergency-launcher.ts: useEmergencyLauncher() — every always-on SOS entry point (bottom-nav center, desktop sidebar, More sheet, Home cards, دليلك السريع) now derives workSafe from userProfile.deviceNeeds (I4). Urge Check keeps its in-the-moment toggle (defaults to profile).
- risk-engine.ts: currentHomeState() gains lastOutcome param — "handled"/"acted" checks no longer paint Home red for 3h (C2); "pending"/"escalated" remain live.
- store.ts: importData() no longer forces screen:"home" — the restore UI's host screen stays mounted so the success confirmation persists (part of restore-success polish).
- HomeScreen.tsx: (C2) passes last check outcome; (C3) NEW compact "دليلك السريع — ماذا أفعل الآن؟" card in stable state only — 4 tappable rows (مستقر؟→الجرعة اليومية / بدأت رغبة؟→افحص الرغبة / قربت من التنفيذ؟→تدخّل الآن / حدث تعثر؟→مركز التعثر) with dotted leaders, 22 words total; (I9) loud red normal-state emergency card REPLACED by the quiet guide row (persistent bottom-nav SOS remains the loud entry); handled-recently acknowledgment ("أحسنت، تعاملت مع موجة منذ قليل"); (I4) high-state button uses launcher + passes lastCheck.triggers; plan pillars → correct icons (Brain/Dumbbell/Target/Users/Heart/Smartphone) and now tappable → plan; post-relapse banner reworded ("التعثر لا يمحو ما تعلمته — والمراجعة الهادئة تنتظرك حين تهدأ").
- UrgeScreen.tsx: (C1) derived checkResolvedExternally — result/outcome views suppressed once the saved check is handled/acted (Emergency closes it under the overlay) → fresh input view, no setState-in-effect; (I1) trigger chips collapsed into <details> (default closed), compute CTA sticky bottom-20 with gradient backdrop, h-14 shadow-xl, disclaimer moved above CTA; (I7) self-reported indicator disclaimer added under the result number + label "مستوى الخطر (Risk)" → "مستوى الخطر"; (I8) "بلغني وضع الطوارئ" → "ابدأ وضع الطوارئ بدلًا منه", "تدخّل الآن — Emergency Mode" → "تدخّل الآن — وضع الطوارئ"; (I10) level-4/5 interrupt copy de-catastrophized ("الرغبة في بدايتها — التقطها مبكرًا: خطوة قطع صغيرة الآن تكفي غالبًا").
- shared.tsx NumberScale + PlanScreen evening 1-10 scale: grid-cols-5 on phones / flex row ≥sm (I2) — 46px targets at 320px (was ~20px).
- RelapseScreen.tsx STOP view: top-aligned + sticky bottom action bar (CTA + skip link) — STOP CTA visible without scrolling at 568px viewport (I3; was y=707 → now y=395).
- EmergencyMode.tsx: completion now closes recent "escalated" checks too (not only "pending") — companion fix for C1/C2 (escalated-from-urge checks were never closed).
- AppShell.tsx: launcher for all SOS entries (I4); More-sheet card title "وضع الطوارئ" → "تدخّل الآن" with "وضع الطوارئ" in desc (I8).
- taxonomy.ts: MODE_LABELS.emergency → "وضع الطوارئ" (no English mix).
- SettingsScreen.tsx: removed placebo toggles "دعم ما بعد التعثر" + "التذكيرات" and requestNotifications() (I5); import dialog no longer auto-closes after 900ms — persistent success.
- RestoreBackup.tsx: removed custom aria-describedby/description id (Radix auto-association restored — warning gone); richer persistent success banner ("أغلق هذه النافذة وستجد كل شيء في مكانه"); restore disabled after success (no double import).
- ValuesScreen.tsx: values draft persisted to dedicated localStorage key istiaada-values-draft-v1 (outside AppData — no backup schema change) with validation + graceful fallback (I6); note updated; typo fixed ("من صغتها بعمق في «قاعدة المعرفة» موضوع القيم" → "صُغها بعمق — ستجد موضوع «القيم» في قاعدة المعرفة").
- DoseScreen.tsx: visible "التخطي ليس فشلًا — الجرعة القادمة بانتظارك" line under done/skip buttons (was tooltip-only).
- PlanScreen.tsx: InfoNote now explicitly says "والتخطي ليس فشلًا، بل حكمة اليوم الصعب".

Verification (all passed):
- tsc --noEmit: 0 errors in src (examples/skills env files excluded — pre-existing, untouched). ESLint src: clean (React Compiler set-state-in-effect avoided by deriving state instead of an effect). Unit: test-risk 15/15, test-backup 19/19. Production build: compiled + static export OK; standalone server 200.
- Browser E2E at 390/360/320: (1) stable Home shows دليلك السريع (4 rows) + dose/plan/progress, 0 console errors; (2) urge 5/5/5 → intervention → handled → Home stable + acknowledgment, store outcome "handled", no stale red banner; (3) urge 9/9/9 → level 10 → Emergency (Work-Safe step 1 verified) → completed → urge screen reset to fresh input (C1 ✓), Home stable with handled ack (C2 ✓), level-10 check closed as "handled"; (4) relapse STOP at 320×568 — CTA y=395 visible without scroll (I3 ✓), full stop→quick→reframe→home journey, post-relapse banner shows improved wording, event persisted; (5) bottom-nav SOS launches Work-Safe Emergency from deviceNeeds=yes profile (I4 ✓); (6) valid backup restore — preview with export date, confirm gate exact wording, NO Radix warning in console, persistent success banner (dialog stays open), restore button disabled after success, state restored (checks=3, relapses=1), close → still on Settings; (7) invalid backup → clear Arabic error + restore disabled + state untouched; (8) reload → onboarding skipped, app opens directly (returning-user persistence intact).
- Mobile sweep 320/360/390 × 11 screens: zero horizontal overflow anywhere; NumberScale 46px targets at 320; urge CTA visible without scrolling; triggers collapsed; desktop 1280 OK.
- Home-not-documentation: guide card = 4 rows / 22 words / 277px at y=187; dose card still in first viewport (y=509 on 844px); whole Home = 153 words.
- Screenshots: tool-results/fix-ux/01..23.

Stage Summary:
- All approved findings implemented with minimal targeted changes; no redesign, no new features, no visual-identity change, no new recovery mechanics. Key UX outcome now self-evident in UI: Stable → guide row 1 (dose), Urge started → row 2 (Urge Check), Close to acting → row 3 (Intervene Now / Emergency), Slip → row 4 (Relapse Center). Skipped per user: global numeral-system normalization.

---
Task ID: 6
Agent: Super Z (main agent)
Task: ADDITIONAL UX + ARABIC COPY + GUIDANCE PASS for استعادة (Istiaada) — Arabic copy quality, terminology design, contextual guidance (four-situation mental model), spiritual-toggle RTL fix. No redesign, no new features, no philosophy change.

Work Log:
- Read ALL user-facing code (12 screens, AppShell, Onboarding, shared components, taxonomy, interventions, knowledge data, engines) and built a full terminology review before touching anything.
- TOGGLE BUG (root cause): ui/switch.tsx thumb used physical `data-[state=checked]:translate-x-[calc(100%-2px)]` — in RTL the thumb starts at the RIGHT edge, so ON pushed it ~14px OUTSIDE the 32px track. Fixed with mutually exclusive `ltr:`/`rtl:` variants (rtl checked = -translate-x) — deterministic regardless of stylesheet order; switch style/size untouched. Verified by geometry measurement: ON 42–58px & OFF 56–72px inside 41–73px track at 390/360/320, dark + light themes, round-trip clicks.
- TERMINOLOGY (old → new): مركز التعثر → ما بعد التعثر (situation-named, not institution); احسب مستوى الخطر → اعرف أنسب خطوة (calculator → step-finder); الاستجابة الموصى بها → أنسب خطوة الآن; mode labels وعي ووقاية → مراقبة هادئة, أقصى تدخل → وضع الأزمة; نفّذت السلوك → رجعت للسلوك; سنصعّد التدخل/صعّدنا التدخل → نجرّب/تدخل أقوى; مؤشر تدخل ذاتي التقرير → plain "تقديرك عن لحظتك / تقدير مبني على إجاباتك"; If–Then → «إذا… إذن» everywhere user-facing (nav, prevention, onboarding, knowledge cards, suggested rules); removed English artifacts (Risk Forecast)/(Check-in)/(Work-Safe)/(Site Blockers)/(Work Profile) parentheticals + Settings localStorage-key line; RISK_LEVELS: 4 محفز نشِط → رغبة تتزايد, level 7/8 descriptions de-jargoned ("السلسلة" removed); tawbah "لا تحتبل الإحباط" broken word → "بالهمة، لا بالانكسار"; intervention name إقفال بيئة العمل (Work-Safe) → إقفال بيئة العمل.
- SCALE LABEL BUGS fixed: urge low anchor "بالكامل موجودة" (said the opposite) → "مش موجودة تقريبًا"; control low "مسيطرة قوية" (scrambled) → "سيطرتي كاملة" + label "٣ · فقدان السيطرة" + natural high anchor.
- CONTEXTUAL GUIDANCE: Home stable note now dose-aware + permission-giving ("أنت مستقر دلوقتي — مش محتاج أي تدخل … ده كفاية"; without dose: "اختار مهمة واحدة من خطة اليوم"); QuickGuide retitled "أعمل إيه دلوقتي؟", rows "قربت تتصرف؟ → تدخّل الآن" / "رجعت للسلوك؟ → ما بعد التعثر" + footer "كل أداة ليها وقتها — استخدم اللي محتاجه دلوقتي"; moderate/high/post-relapse banners rewritten in plain language; Urge subtitle = bridge framing ("بدأت الرغبة؟ افحص اللي حاصل — عشان تعرف أنسب خطوة. الرغبة إحساس، مش أمر"); Emergency header "ما تحللش دلوقتي. نفّذ الخطوة الحالية فقط" + step2 natural ("مكان فيه ناس أو حركة — ومش لازم تخبر حد"); Relapse STOP = "رجعت للسلوك؟ / ما تكملش — الوقفة هنا أهم خطوة" + 6 plain steps (no "السلسلة"/"المحفز" at the urgent moment); reframe lines ("تعثر واحد مش يوم ضايع، ولا إذن بالتكملة"); wizard questions de-jargoned (الحدث/الأمر instead of السلسلة); knowledge screen one quiet "when-not" line ("للقراءة في الهدوء — وقت الشدة له أداة أسرع: تدخّل الآن"); TriggerMap subtitle now defines محفز in one line; NAV descs rewritten as when-to-use lines.
- Fixed self-introduced typo مستقبرك → مستقبلك; updated mobile-sweep.sh label for renamed section.

Verification (all passed, production build on :3100):
- tsc --noEmit clean (src), eslint clean, unit tests 15/15 + 19/19, production build compiled + standalone server 200.
- Dev server recovered from stale corrupted .next/dev cache (was 500/ENOENT before this task); now GET / 200.
- 6 no-onboarding scenarios: (1) stable Home tells user exactly what to do + permission ("مش محتاج أي تدخل… ده كفاية") + guide; (2) urge screen bridges to step-finder — low check → "أنسب خطوة الآن: مراقبة هادئة" + plain self-report line; (3) 9/9/9 → level 10 "أزمة فورية"/وضع الأزمة → single CTA → Emergency "ما تحللش دلوقتي" → full 4-step flow → "هبط الخطر — أحسنت. سجّلنا إنك تعاملت مع الموجة" → Home stable + أحسنت ack + store outcome=handled (C1/C2 intact); (4) relapse: ما بعد التعثر → STOP plain header/steps → quick log → reframe → wizard → suggested rule "إذا بدأ الأمر من ملل → أتدخل مبكرًا…" ; (5) More sheet descs teach tool-for-situation (trigger map / knowledge); (6) knowledge quiet line + stable Home creates no busywork.
- Toggle: ON/OFF × RTL × 320/360/390 × dark/light — knob always inside track (measured), correct direction (ON=left in RTL), gating works, clickable.
- Mobile sweep 11 screens × 320/360/390: zero horizontal overflow; urge CTA & relapse STOP CTA visible without scroll at 320×568 (top=428 / top=115 on 568px viewport); SOS bottom-nav launch works; 0 page errors, 0 console warnings through the whole session.
- Screenshots: tool-results/copy-01..19.

Stage Summary:
- The four-situation mental model is now fully self-explanatory in natural MSA/Egyptian mix without onboarding dependency: stable → dose+one task ("ده كفاية"), urge started → فحص الرغبة (step-finder), close to acting → تدخّل الآن ("ما تحللش دلوقتي"), returned to behavior → ما بعد التعثر ("ما تكملش — نوقف هنا الأول"). Spiritual toggle RTL bug fixed at root. No redesign, no new features, no recovery-philosophy change; all previously approved fixes and flows regression-verified intact.

---
Task ID: 7
Agent: Super Z (main agent)
Task: DEEP UI/UX + PRODUCT + BEHAVIORAL VALUE AUDIT (16-part brief) of the current post-fix state of استعادة — READ-ONLY, no code changes, report + roadmap, wait for approval.

Work Log:
- Read full worklog (tasks 1–6) to reconstruct history: app built → audit #1 → fixes #1 → behavioral audit #2 (C1–C3, I1–I10, polish) → fixes #2 → Arabic copy/terminology/guidance pass #3. Current state = post all fixes.
- Deep code read: all 12 screens, AppShell/AppRoot/Onboarding/RestoreBackup/shared/InterventionCard/Timer, store/risk-engine/progress/dose-engine/intervention-engine/helpers/backup/emergency-launcher, taxonomy + interventions (full) + knowledge-core (sample) + counts (98 items, 5 spiritual), layout.tsx/next.config.ts.
- Live browser audit at 390x844 + 320x568 + 1280: full fresh onboarding walkthrough; stable Home comprehension check (QuickGuide 4 rows + permission line); urge check 5/5/5 → level 5 → intervention → handled → Home ack (C1/C2 verified live); 9/9/9 → level 10 أزمة فورية → single CTA → Emergency 4-step flow → success → urge screen reset to fresh input + check closed handled (C1 verified); relapse STOP at 320 (CTA y=395 visible without scroll, I3 verified) → quick log → reframe → post-relapse Home banner + dose engine switched to relapse content; spiritual toggle ON at 320 — thumb x=42–58 inside track 41–73, ON=left in RTL (RTL fix verified); mobile sweep 11 screens × 320/360/390 — zero horizontal overflow; desktop clean; 0 page errors / 0 console warnings all session.
- Automation frictions diagnosed and classified as NON-app issues: smooth scroll-behavior racing agent-browser hit-tests; Next.js devtools portal covering nav at 320 (dev-only).
- Findings (genuine, post-fix): 🔴 personal "why" promised in 4 UI strings but never rendered outside ValuesScreen (motivation gap + broken promise); 🟠 post-action dead-end (high-state Home offers only تدخّل/أعد الفحص, no ما بعد التعثر route; same after exiting Emergency mid-crisis); 🟠 evening check-in "تعديل" opens with defaults and overwrites saved answers; 🟡 Emergency overlay lacks focus trap/initial focus (custom alertdialog, background not inert); 🟡 escalation has no terminal state; 🟢 typo خارٍج→خارج in Work-Safe step (rendered in Emergency); 🟢 dead settings fields persisted; 🟢 knowledge search lacks Arabic normalization; 🟢 handled-ack wording window 3h; 🟢 "تصميم طبي" phrasing; notes: no PWA manifest/SW (needs network to load), typescript ignoreBuildErrors:true, values draft excluded from backups (documented in UI).
- NO code modified (audit only).

Stage Summary:
- Post-fix app is fundamentally strong: four-situation model fully self-explanatory, all previously approved fixes verified holding in live UI. Remaining highest-value work: surface user's own "why" in difficult moments (unused asset + unfulfilled promise), post-action escape route from high state, check-in edit hydration, emergency focus management, PWA offline. Full 16-part report + Tier 1/2/3 roadmap delivered in chat; awaiting user approval before any implementation.

---
Task ID: 8
Agent: Super Z (main agent)
Task: 1–10 → 1–5 scale redesign (system-wide) + P1–P4 fixes + copy/small fixes

Work Log:
- Redesigned risk-engine.ts around 1–5 inputs: weighted base (0.45·p + 0.30·c + 0.25·u), context bonus capped +0.75, arithmetic clamped to [1,4], behavioral floors (browsingStarted→≥4; p≥4 or c=5→≥4), Level 5 ONLY at p=5 AND c=5 (urge plays no part). Mode map: 1–2 awareness, 3 interrupt, 4 immediate (emergency when browsingStarted), 5 maximum.
- taxonomy.ts: RISK_LEVELS now 5 behavioral levels (هدوء/بداية بسيطة/بدأت تقوى/خطر مرتفع/على وشك التصرف); RISK_MODES remapped; fixed "خارٍج"→"خارج".
- interventions.ts: all 28 riskLevels re-fit to 1–5; intervention-engine/dose-engine/progress thresholds updated (≥4 high zone, 3 growing zone, early = ≤3).
- NEW src/lib/app/scale.ts: documented band mapping ceil(old/2) (1–2→1 … 9–10→5) + migrateAppDataToScale5.
- Backward compat: backup.ts era detection (appVersion <2.2.0 → legacy; fallback any value >5) + normalization on import; validators intentionally accept 1–10. APP_VERSION → 2.2.0, backup schema stays 1. store.ts persist version 1→2 with migrate() for live localStorage.
- UI: NumberScale 1–5 (44×48px targets), UrgeScreen (درجة حالتك الآن / X من ٥, per-level copy), Evening check-in highestUrge 1–5, EmergencyMode defaults 4 / maximum=≥5 / درجة الحالة header, HomeScreen copy + thresholds, ProgressScreen/PreventionScreen copy.
- P1: PersonalWhy block (why text first, chips fallback, nothing when empty) on emergency step-4 (non-maximum) + done screen; never on level-5 crisis or steps 1–3.
- P2: "رجعت للسلوك؟ ما تكملش — نوقف هنا الأول" entries on Home high card (under divider) + emergency done screen → navigate("relapse").
- P3: EveningCheckInDialog preloads existing check-in on open (loadFrom); partial edits preserve everything else verbatim.
- P4: EmergencyFrame keyed per branch (remount = refocus), data-autofocus on primary CTAs (skips timer on step 3), aria-modal, Tab trap, #app-shell-root inert+aria-hidden, focus restore on close; no Escape binding. Fixed key placement bug found in E2E (key must be on <EmergencyFrame> element, not inner div).
- Copy: "تصميم طبي"→"تصميم يناسب"; "من قليل" time-sensitive (outcomeTs??ts: <45m/45–110m/≥110m); knowledge scale reference "مستوى خطر ٣-٤"→"درجة ٢ أو ٣"; helpers.ts normalizeArabic() applied to Knowledge search query+haystack.
- Tests: test-risk.ts rewritten (43 checks); test-backup.ts extended (53 checks incl. legacy import matrix).

Stage Summary:
- All approved scope implemented; nothing from the defer-list touched.
- Verified: tsc clean (src), ESLint clean, 96 unit checks pass, production build passes, mobile sweep 11 screens × 320/360/390 no overflow, legacy backup E2E import migrated correctly, four-state model intact, P1–P4 verified in browser, Work-Safe intact.

---
Task ID: 9
Agent: Super Z (main agent)
Task: Evidence-cleanup pass — regenerate the final key UI screenshots on the 1–5 implementation, replace/archive stale 1–10 evidence, confirm "من ٥" everywhere appropriate and that the live UI has zero user-facing 1–10 references. NO application changes.

Work Log:
- Audited evidence state: tool-results/archive-pre-scale5/ already held all pre-migration 1–10 screenshots (107 PNG + fix-ux 23 + 3 VLM JSONs, README accurate — counts verified); tool-results/scale5/ held an unverified set from the interrupted previous run (VLM report 9 failures: 6× API-429, 3 misjudgments); scale5/README.md was missing (referenced by archive README); download/scale5-screenshots/ was empty.
- Source sweep: all "1–10" matches in src/ are internal migration-documentation comments (scale.ts/backup.ts/store.ts) or Tailwind opacity classes (bg-primary/10) — zero user-facing 1–10 strings; "من ٥" confirmed in UrgeScreen/EmergencyMode/ProgressScreen/aria-labels; SCALE_MAX=5.
- Built evidence tooling (scripts/ only, no app changes): make-scale5-seed.mjs (realistic 21-day seeded history: why text, handled checks, intervention logs, 4 evening check-ins, reviewed relapse), shot-scale5.sh (agent-browser driver walking all key flows live: level 2/3/4/5 checks, full emergency path, relapse stop→quick log→reframe, evening check-in save + hydrated re-edit; 22 shots at 390/320/1280), verify-scale5-shots.ts upgraded (429 retry/backoff 15s/60s/135s, per-file factual hints so durations like "~10 دقيقة" are not misread as scale values, filtered re-run merge, CLI dir arg), check-dev-badge.ts, sweep-live-1-10.sh (live DOM sweep of all 11 screens).
- Dev-server capture first (22/22 after fixes: shot 19 re-centered on the 1–5 row; shot 13 duration false-positive corrected via hint), but VLM confirmed the Next.js dev badge was visible in dev shots → rebuilt production (next build + standalone on :3100) and re-captured all 22 shots from the production build inside one shell invocation (sandbox kills background servers between calls). Production run: every step clean, 0 page errors, 0 console warnings, no dev badge.
- VLM verification of the production set: 22/22 passed — no legacy 1–10 trace anywhere; REQUIRED_OF5 files (04, 05, 06, 07, 08, 09, 17, 18b, 19, 21) all show the 1–5 ladder ("من ٥", "سلم من ١ لـ ٥", "X/5", or exactly-five option rows); 21 confirms P3 hydration ("3/5" reload).
- Evidence swap: old unverified scale5 set deleted (superseded duplicate), production set + vlm-verify.json moved in; wrote the missing scale5/README.md (coverage table, provenance, verification status); updated archive-pre-scale5/README.md "What replaced them" section; published the deliverable copy to download/scale5-screenshots/ (22 PNG + README + vlm-verify.json) and rewrote download/README.md.
- Live-UI confirmation (3 independent layers): (1) source sweep clean; (2) VLM pass over all key screens; (3) live DOM sweep across all 11 sections — legacy markers [] and wide radiogroups [] everywhere; every "10/١٠" occurrence is a duration or clock time (١٠ دقائق، بعد ١٠ مساءً، ٥م–١٠م).
- Verified zero application files touched this pass (src mtimes all predate it — last changes were Task 8's migration at 21:40–21:53).

Stage Summary:
- Project evidence now has a single authoritative, machine-verified source of truth: tool-results/scale5/ (22 production-build screenshots, VLM 22/22, "من ٥" wherever a scale is visible) mirrored to download/scale5-screenshots/ for the user. All pre-migration 1–10 screenshots remain clearly quarantined in tool-results/archive-pre-scale5/ with an explanatory README, and the previous unverified intermediate set was replaced. The live UI is confirmed free of user-facing 1–10 references across source, pixels, and DOM.

---
Task ID: 10
Agent: Super Z (main agent)
Task: FINAL copy pass — replace "ما بعد التعثر" primary CTA/nav labels with an action-oriented phrase + small first-time-user clarity pass on other primary actions. Copy only: no logic, no scale, no redesign, no features.

Work Log:
- Reviewed every occurrence of "ما بعد التعثر" (4 user-facing spots: AppShell NAV_ITEMS label+desc, HomeScreen QuickGuide row-4 action, HomeScreen post-relapse banner CTA, RelapseScreen main title) plus all other primary CTAs/labels against the rule "a first-time user must know WHAT TO DO without internal terminology".
- Chose "توقّف هنا" (calm imperative, matches the screen's own STOP language "ما تكملش — الوقفة هنا أهم خطوة", zero jargon, works at nav scale). Nav desc → "رجعت للسلوك؟ إيقاف فوري ثم فهم هادئ" (teaches the two-stage flow, avoids توقّف repetition).
- QuickGuide row-1 action "الجرعة اليومية" → "خد جرعة اليوم" (guide rows are actions; matches the app's existing stable-note phrasing "خد جرعة اليوم واختار مهمة واحدة").
- Kept untouched: "رجعت للسلوك؟" questions, EmergencyMode P2 entry "رجعت للسلوك؟ ما تكملش — نوقف هنا الأول" (already action-oriented), all educational "التعثر" contexts (سجل التعثرات، بعد التعثر: تذكير مهم، reframe lines, Home post-relapse banner header) per user instruction.
- mobile-sweep.sh label list updated ("ما بعد التعثر" → "توقّف هنا") — tooling only.

Verification (production standalone :3100): guide row 4 "توقّف هنا" ✓, row 1 "خد جرعة اليوم" ✓, old label absent from DOM ✓, more-sheet card label+desc ✓ (clicks through), RelapseScreen title ✓, STOP header ✓, post-relapse banner CTA ✓; mobile sweep 11 screens × 320/360/390 zero overflow; 0 console errors.

Stage Summary:
- Primary post-behavior destination is now named by what to DO ("توقّف هنا"), not by recovery vocabulary; "التعثر" survives only in explained/educational contexts. No logic/scale/design changes.

---
Task ID: 11
Agent: Super Z (main agent)
Task: FINAL BRANDING ADDITION — design + implement the Istiaada logo («استعادة») and favicon. Branding addition only: no UI redesign, no new visual system.

Work Log:
- CONCEPT «حلقة العودة» (The Return Loop): one continuous stroke — an open loop whose returning end comes back through the opening toward its own center (استعادة = taking back / returning to balance); gap stays open on purpose (recovery in progress, not a closed circle); echoes Arabic letter ع (root of عَوْد). No shield/check/heart/lock, no medical imagery, no shame symbols. Geometry (48 viewBox): ring center (24,24.5) R=14.5 stroke 6 round caps, gap at screen angles 145°–215°, main arc 290°, tangent-continuous inner tail arc r=6.5 → 250°. Path: M 12.12 16.18 A 14.5 14.5 0 1 1 12.12 32.82 A 6.5 6.5 0 0 1 15.22 22.98.
- scripts/make-brand-assets.mjs (sharp + parametric geometry + OKLCH→sRGB): brand teals computed from globals.css — light primary #268676, dark primary #5AC0AB, badge bg #131E21 (viewport themeColor). Replaced the generic Z-placeholder public/logo.svg with the real mark.
- LogoMark.tsx: inline SVG, currentColor (theme-adaptive), aria-hidden decorative, fixed viewBox (no layout shift). Integrated in: AppShell sidebar brand tile (same size-10 bg-primary tile — geometry unchanged, content swap only), Onboarding brand bar (size-11 tile), Onboarding welcome hero (size-16 bg-primary/10 tile, replacing generic Sparkles). Nowhere else — discreet.
- FAVICON: public/icon.svg (64-viewBox rounded badge), public/favicon-32.png (32×32), public/apple-touch-icon.png (180×180 square), wired via explicit metadata.icons in layout.tsx. Root cause found the hard way: PNG metadata-route files (src/app/icon.png/apple-icon.png) crash Turbopack 16 builds ("Dependency tracking is disabled…" panic, reproducible, gone without them) → public/ + explicit icons = identical browser behavior, zero metadata-route processing.
- Verification infrastructure: scripts/verify-brand.sh + verify-brand-light.sh (production standalone :3100; pre-kills ghost servers; standalone server.js needs PORT env — an old ghost :3100 served stale-chunk error pages on the first run, diagnosed and fixed).
- VERIFIED (production build, standalone :3100): icons HTTP 200 with correct content-types (svg 346B / png 32×32 / png 180×180) + exact link tags in served HTML; favicon recognizable at 16px AND 32px on simulated light AND dark tab strips (VLM-confirmed, tabstrip-*.png); Chrome's own rendering of the served /icon.svg captured (chrome-icon-16/64.png); loop mark confirmed live in onboarding hero + brand bar (390 + 320, no overflow) and desktop sidebar dark + light (light captured via the app's real mechanism — persisted settings.theme, which AppRoot syncs into next-themes; naive localStorage istiaada-theme edit gets overridden); mobile sweep 11 screens × 320/360/390 zero overflow; 0 console errors; tsc clean (src), eslint clean, unit 43/43 + 53/53; production build passes.
- Published user copy: download/istiaada-brand/ (logo.svg, icon.svg, favicon-32.png, apple-touch-icon.png, mark-256.png, tabstrip renders, 3 live screenshots, README with concept + integration notes).

Stage Summary:
- Istiaada now has one real identity mark, consistent across sidebar / onboarding / browser tab / iOS home screen, adapting to both themes through currentColor, inside the existing design system (same tiles, same teals — no redesign). Turbopack-vs-PNG-metadata-route build crash documented and routed around. Existing visual identity fully preserved; only the generic "اس" text tile and Z-placeholder logo.svg were replaced.

---
Task ID: 12
Agent: Super Z (main agent)
Task: PHASE 2C (Arabic Copy + Terminology + UX Language) + PHASE 3 (Knowledge Base rewrite of the 98 cards) — one-shot End-to-End run: audit → analysis → implementation → content rewrite → consistency review → tests → E2E → build → visual QA → self-review → fix → final report. No new topics, no 50 additions, no semantics reversal of approved 2B.2 model.

Work Log:
- Read the full worklog + the approved `upload/phase2b2-relapse-semantics-review-approved.md` + ALL user-facing code (12 screens, AppShell/Onboarding/RestoreBackup/shared/InterventionCard/Timer, taxonomy + 28 interventions + 98 knowledge cards, all engines and helpers, backup/store/types).
- DISCOVERED REAL REGRESSION: the approved 2B.2 fields (classification/behaviors) were entirely absent from the code (last commit predates 2B.2 approval). Restored the baseline minimally per the approved doc: types.ts (RelapseClassification/RelapseBehavior optional fields — legacy records never guessed), backup.ts (strict validators incl. uniqueness + round-trip safe, APP_VERSION 2.3.0), RelapseScreen quick form (behavior multi-chips + زَلّة/انتكاسة classification cards, required-before-save, full form reset after save = fix1), classification-aware reframe using the approved phrasing (سجّلناها كزَلّة / سجّلناها كانتكاسة), event list + pending-review rows show classification/behavior chips. No auto relapse detection, no time windows, no engine changes.
- PHASE 2C implementation across all surfaces: Home (guide row 4 + high-state link + post-behavior banner + days-since wording), AppShell nav desc, EmergencyMode P2 link, UrgeScreen acted outcome (نفّذت السلوك), DoseScreen reason line, ProgressScreen (مراجعات متتالية، أوقفت عند أولها، بعد السلوك), TriggerMapScreen, KnowledgeScreen subtitle, RestoreBackup summary line, backup error strings, taxonomy (MODE_LABELS.maximum → أزمة — تدخل أقصى; RISK_LEVELS[5] person-neutral description; anti-rationalization already-slipped; tawbah body), knowledge.ts category label التعثر → الزلّة والانتكاسة, Onboarding (removed gratuitous نحن voice), shared.tsx aria من ٥, package.json 2.3.0.
- PHASE 3 rewrite of all 98 cards via scripts/phase3-knowledge-rewrite.py (121 asserted replacements): terminology unification (زَلّة/انتكاسة canonical), real typo fixes (نع نهار/سمسرة/العطر، الحادرة، الجهزية، الصافح، إخماق، فحظة، وغالبة، صدقة، ضيف، مشطته…), translationese removal, leftover 1–10 scale bug in ur-surfing-skill (٣-٥ → ١–٢), flipped-meaning fix in lt-identity-keeping (تحرس النمط القديم → تحمي النمط الجديد), inverted-logic fix in pv-friction, Latin X placeholders → Arabic examples, SafeSearch → بحث آمن, person-judgment softening (لا لأنك ضعيف → لا بسبب ضعف فيك etc.). IDs/categories/tags/stages/spiritual flags untouched; 98/98 unique; deep field still only on bb-dopamine (no invented content).
- Deliverables generated: download/phase3-knowledge-98-final.md (all 98 cards, full fields incl. Original Title + غير موجود for missing deep), download/phase3-knowledge-change-map.md (70 language-changed / 28 intentionally unchanged / 5 title changes / 5 documented semantic-corrections), download/phase3-knowledge-gap-analysis.md (strong/partial/missing + 10 future topic families — NO cards created), download/phase2c-copy-final-audit.md (full audit surface table + terminology decisions + no-change list).
- Tests: test-backup.ts extended with 14 new checks for the 2B.2 fields (accept/preserve/reject invalid/empty/duplicate, legacy stays undefined, round-trip) → 66/66; test-risk 43/43.
- E2E (scripts/e2e-phase23.sh, production standalone :3100): 33/33 — new relapse flow (behaviors+classification, save gating, persistence, reframe phrasing, chips), form reset after save, urge 5/5/5 → level 5 → emergency full path → check closed handled + post-behavior banner precedence, knowledge category chip (6)/spiritual gating (0↔5)/diacritic-insensitive search (الزلة→10, انتكاسة→9)/rewritten card content, progress terminology, backup import round-trip with new fields (preview زلات وانتكاسات، confirm gate, success, persisted relapse classification+both behaviors), zero page errors. Mobile sweep 11 screens × 320/360/390: zero overflow.
- Visual QA (scripts/visual-qa-phase23.ts, VLM): 22/22 screenshots passed (RTL, no clipping, mixed text, punctuation, new copy renders) — tool-results/e2e-phase23/vlm-visual-qa.json + 22 PNGs.
- Self-review sweeps: zero remaining رجعت للسلوك؟/وضع الأزمة/الحدث/واقعة/سلسلة-continuation in user-facing src; fixed PlanScreen aria من ٥; re-generated the 98-final doc with Original Title per spec.
- Final verification: tsc scoped src = 0 errors (pre-existing errors remain in examples/scripts/skills — outside scope), ESLint src clean, builds pass (production compiled).

Stage Summary:
- Phase 2C closed: terminology (زَلّة/انتكاسة/السلوك) applied to every surface with traceability; register distribution preserved (Egyptian for urgency, light MSA for education); RTL clean; the missing 2B.2 baseline restored minimally and is now export/import-safe.
- Phase 3 closed: all 98 cards rewritten in code + exported fully (98-final), change map with per-card traceability, gap analysis WITHOUT creating any new topics — the 50 remain deferred to Product Owner.

---
Task ID: 13
Agent: Super Z (main agent)
Task: PHASE 3A/3B ONE-SHOT — Native Arabic Rewrite of the 98 knowledge cards + add the 50 new topics from phase3-knowledge-50-topics-proposed.md + integrate into the app + consistency pass + evidence review + tests + E2E + visual QA + self-review + fix + final report. No UX/business-logic redesign; no topics outside the approved list; no semantic reversal.

Work Log:
- Read all available reference docs (2B.2 approved semantics, 2C final audit, 98-final, change-map, gap-analysis, 50-topics proposal) + all knowledge source code + dose-engine + KnowledgeScreen + taxonomy triggers + e2e/visual-qa scripts. NOTE: phase2a-copy-inventory.md / phase2b-language-terminology-map.md / phase2b1-relapse-semantics-audit.md are NOT present in the workspace (only in earlier session context); their substance is embodied in the 2B.2-approved + 2C-audit docs and worklog Task 12 — treated as source of truth, recorded in final report.
- Snapshot of the pre-rewrite 98 saved to scripts/native-pre/ for before/after traceability.
- PHASE 3A — native rewrite of all 98 in place (knowledge-core/wellbeing/recovery.ts): re-authored every sentence from the meaning (verbal sentence flow, natural collocations, Arabic conjunctions replacing +/=/→ notation, de-calqued English idioms like "غيّر اللعبة"/"الطاولة محسومة"/"فاتورة بطيئة"/"لا تستأجره مجانًا", Arabic numerals ١١م→الحادية عشرة, voice neutralization "قلّصنا لك"→neutral, softening "معايير إعدام"→"معايير قاسية"). IDs/categories/tags/stages/spiritual flags untouched; 96/98 cards changed textually, 2 already-native cards untouched (hl-implementation, dg-no-notifications); 5 title changes (symbol-notation titles → Arabic). De-duplicated canonical phrases: "الفضول ليس أمرًا بالبحث" now only in tr-curiosity; "لا تختبر نفسك أمام المحفز" now only in dg-no-testing. ZERO semantic changes (no recommended-behavior/certainty/purpose changes); 2 aphorism swaps documented as meaning-preserving de-duplication.
- PHASE 3B — created src/data/app/knowledge-new.ts with exactly 50 new cards (NEW-01..NEW-50 mapped to semantic ids: di-morning-protocol … re-first-appointment), written in native Arabic from scratch, existing categories only, no new spiritual-gated cards (Ramadan card is behavioral/sleep-only), no medical/sexual claims, tags drawn from taxonomy trigger ids. Merged into KNOWLEDGE via knowledge.ts — flows automatically into Knowledge screen, search, category chips, and dose engine (no custom per-card logic).
- Content validation script scripts/validate-knowledge.ts: 15/15 (148 total, unique ids, required fields, categories/stages valid, spiritual gate = original 5 sp-* only, all 50 NEW ids present, old 98 distinct, زَلّة diacritics consistent, no judgmental phrases, no symbol notation, no stray Latin).
- E2E updated + extended (scripts/e2e-phase23.sh): relapse chip 6→9, الكل chip 143 (148−5 gated, spiritual OFF), rl-lapse-relapse native definitions, NEW-40 card opens (fixed hidden pre-existing bug: the old "إغلاق" click never worked — the dialog has no such button, only Radix "Close"; masked before by a reload), NEW-09 search «رمضان»=1, Daily Dose renders knowledge card from 148-pool. RESULT: 37/37 passed.
- Unit tests: risk 43/43, backup 66/66. TypeScript scoped to src/: 0 errors (pre-existing errors remain in examples/scripts/skills — out of scope). ESLint src: clean. Production build: pass.
- Visual QA (VLM): 24/24 screenshots incl. new-card dialog + dose screen. Mobile sweep 11 screens × 320/360/390: zero overflow.
- Deliverables generated: download/phase3-knowledge-98-native-arabic-final.md (98 full), phase3-knowledge-50-final.md (50 full with provenance), phase3-knowledge-native-arabic-change-map.md (98 before/after + 50 overlap analysis), phase3-knowledge-final-gap-analysis.md (before/after coverage, 8 documented overlap pairs, deferred areas), phase3-knowledge-evidence-review.md (16 claims flagged for future source review — none blocking, no certainty raised).
- Self-review sweeps: no repeated know-openers, no repeated remember lines, zero calque markers, "ليس أمرًا" family limited to its 3 canonical homes; fixed number-agreement slips (عشرون→٢٠ in two acts) and one colloquialism (بالموجود→بما يقدر عليه).

Stage Summary:
- Knowledge bank now 148 cards (98 natively rewritten + exactly 50 from the approved proposal), same schema/categories/IDs, speaking one product voice. Zero card deletions, zero semantic reversals, zero invented evidence. All QA gates green: validation 15/15, E2E 37/37, VLM 24/24, sweep 33/33 screens, tsc(src) 0, eslint(src) clean, build pass.

---
Task ID: 14
Agent: Super Z (main agent)
Task: FINAL NATIVE ARABIC EDITORIAL PASS — linguistic-only review of all 148 knowledge cards (98 rewritten + 50 new): remove translationese, awkward constructions, and slogan-y phrasing with minimal necessary edits; no content/structure/science changes; docs + full QA.

Work Log:
- Read all 148 cards in full (core 25 / wellbeing 34 / recovery 39 / new 50); verified every user-flagged example exists in current copy; snapshot of the pre-pass state saved to scripts/final-editorial-pre/.
- Editorial pass executed as 75 targeted edit operations across 53 cards (core 14, wellbeing 6, recovery 11, new 22): all 9 confirmed user examples fixed exactly as suggested (يغلو ثمنه، أرخص فرصة، فروتين/فمكافأة، مدى البصر، أغلق على نفسي المستقبلي، القرار المأخوذ منك، أكثر المشاعر سرعة، اقطع أول نقرة تكن، الشدة تصرخ/القرب ينفذ، الموجة العابرة خبر) + the same problem families swept everywhere: market/price metaphors (أرخص/يغلو/الرصيد الأغلى), momentum/anchor calques (زخم، مرساة), fed calque (تُطعَم), incoming-exposure/recommendation-environment/daily-vote/physically/opening-negotiation calques, all five «X، فY، فZ» punctuation chains → «ثم», محكمة/ميدان/سلاح metaphor cleanups, reversed-logic fix in sp-dhikr-anchor, referent bug في tr-no-testing-decision (ضدها→ضدك), ambiguous imperative fix in em-temporary (مرّ الشعور وبقِ القرار → الشعور يمرّ؛ أثر القرار يبقى).
- Deliberately kept (judged natural, not decorative): درجة الغليان، المشكلة الضبابية، صمام/مضخة، نافذة الخطر/التحمل as established product concepts, أرضٌ خصبة, the battle/system metaphors family in di-* cards.
- Structural integrity verified programmatically: IDs/categories/tags/stages/spiritual flags 148/148 identical to pre-pass; numbers/durations untouched; certainty/hedging language untouched; no card added/deleted/merged; terminology list preserved (زَلّة/انتكاسة/أيام متتالية etc.).
- Section-16 pattern sweep after editing: every flagged pattern now 0 matches (the 2 intentional keeps documented); no +/=/→ in Arabic prose; no cross-card phrase duplication (30+ chars); remember/know/understand openers show natural variety (max 2× any prefix).
- Independent second review pass applied the 5 naturalness tests to every edited card in final form → 2 additional micro-refinements (dg-rest-no-phone, at-post-completion).
- Fixed an infra regression discovered during E2E: `rm -rf .next` before rebuild had wiped .next/standalone/.next/static (Next.js standalone requires a manual copy step) → chunks 404 → app never hydrated → 34 spurious E2E failures. Restored via `cp -r .next/static .next/standalone/.next/static` + public; the content edits were innocent (validation/build had passed all along).
- QA all green: validate-knowledge 15/15 · tsc(src) 0 errors · eslint(src) clean · unit 43/43 + 66/66 · production build pass · E2E 37/37 · dedicated knowledge sweep with 5 edited-card dialogs at 320/360/390 (new script scripts/final-editorial-sweep.sh) 39/39 incl. zero horizontal overflow in list/dialogs/search results · general mobile sweep 11 screens × 3 widths zero overflow · VLM visual QA (scripts/visual-qa-final-editorial.ts, incremental with createVision API) 18/18 (RTL, no clipping, natural Arabic typesetting).
- Deliverables: download/phase3-knowledge-148-native-arabic-final.md (all 148 full fields), download/phase3-knowledge-native-final-editorial-map.md (53 before→after with problem types + reasons, 95 unchanged listed), download/phase3-knowledge-native-final-qa.md (counts, invariants, sweep results, all test results); download/README.md updated; stale knowledge-*.tmp.ts leftovers removed from src/data/app/.
- Tooling persisted: scripts/final-editorial-pass.py (the 64-edit table), scripts/final-editorial-sweep.sh, scripts/visual-qa-final-editorial.ts, scripts/final-editorial-generate-docs.ts; evidence in tool-results/final-editorial/ (18 PNG + vlm-visual-qa.json).

Stage Summary:
- All 148 cards now read as natively written Arabic: 53 lightly edited (minimal-necessary-change principle), 95 judged already-native and untouched. Zero changes beyond language — IDs, categories, tags, stages, spiritual gates, schema, numbers, scientific hedging, and behavioral recommendations are bit-identical. Every QA gate green (15/15, 37/37, 39/39, 18/18, 0 overflow, build/tests clean). This closes the last language round for the knowledge bank as requested.

---
Task ID: 15
Agent: Super Z (main agent)
Task: FINAL EVIDENCE VERIFICATION PASS — verify the 16 deferred scientific claims from phase3-knowledge-evidence-review.md with real web research; correct only where evidence requires; preserve locked language/structure/IDs; 5 deliverable docs + full regression (tsc/ESLint/unit/build/E2E/responsive/visual).

Work Log:
- Read the three source files (148-native-arabic-final as Source of Truth, evidence-review with the deferred list, native-final-qa) + full worklog; extracted the 16 reviewable cards (17 rows in the list; row 17 sl-ramadan is documented "no review required — transparency only", so the executable scope = 16 as stated).
- Extracted current wording of all 16 cards to scripts/evidence-cards-extract.md (all 16 found in the 148 file).
- Executed 27 web searches (z-ai web_search), raw results preserved in scripts/evidence-search/ (24 JSON): incentive sensitization (Robinson & Berridge 1993/2008/2016/2025), Wegner 1987/1994, Gollwitzer & Sheeran 2006 (d=0.65, 94 tests), 20-second rule origin (traced to Shawn Achor's The Happiness Advantage — pop book, no peer-reviewed support for the number), sleep meta-analyses (Tomaso 2020; Demos 2016; Chen 2017), phone mere-presence + sleep (Carter 2016 JAMA Pediatrics meta-analysis: "device presence in bedroom even without use associated with worse sleep outcomes"; Ward 2017 Brain Drain = cognition-only; Böttger 2023 meta questioning it), affect labeling (Lieberman 2007; Torre & Lieberman 2018; timing/intensity boundary study), Breines & Chen 2012, social support reviews (Islam 2023), AVE (Marlatt & Gordon 1985; Shiffman 1997), ego depletion RRR (Hagger 2016, DOI 10.1177/1745691616652873, d=0.04), Stothart 2015 notification cost, urge duration (clinical consensus 15-30 min, "no fixed number"), craving decline (Am J Addict 2010;19(6):510-514 doi:10.1111/j.1521-0391.2010.00082.x, 865 outpatients) + incubation caveat (Parvaz 2016).
- Verdicts: 13 SUPPORTED (no change) + 3 PARTIALLY_SUPPORTED. Changes (minimal, evidence-forced only): ur-wave — inserted «غالبًا» before the time range (clinical approximation, not experimental constant); en-friction — «عشرين ثانية» → «ثوانٍ قليلة» (number traces to pop book; friction direction is supported, so «فعليًا» kept); em-labeling — «تخفض شدته فعلًا» → «كثيرًا ما تخفض شدته» (neural effect robust, behavioral effect modest and moderated). All three edits applied identically in code (knowledge-core.ts ×2, knowledge-wellbeing.ts ×1) and in the new 148 doc.
- Generated phase3-knowledge-148-evidence-verified-final.md via scripts/evidence-generate-148.py with strict assertions: 148 cards before/after, IDs/order identical, structural fingerprint (ID/category/tags/stage) identical for all 148, text diffs = exactly the 3 intended cards, header documents the round.
- Wrote the 4 companion deliverables: phase3-knowledge-evidence-final.md (16 claim-by-claim records: verdict/evidence/limitations/required change/final wording/reason), phase3-knowledge-evidence-sources.md (30 actually-used sources; DOIs/PMID only where literally confirmed: Ward 10.1086/691462, Hagger 10.1177/1745691616652873, AmJAddict 10.1111/j.1521-0391.2010.00082.x PMID 20958846), phase3-knowledge-evidence-change-map.md (3 before/after/why/evidence + 13 NO CHANGE records), phase3-knowledge-evidence-qa.md (full report). Updated download/README.md.
- ENVIRONMENT REPAIR (documented, not content): 4 dead pre-task files breaking tsc(src) — stale knowledge-*.tmp.ts snapshots + src/lib/db.ts scaffold leftover (all predating this task; removal documented in Tasks 3/14, reappearance = environment restoration artifact) — moved to tool-results/evidence-env-backup/; tsc(src) then 0 errors. Pre-existing scaffold leftovers with no verification impact (api/route.ts "Hello world", prisma/, db/) left in place and flagged in QA.
- Regression: validate-knowledge 15/15 · tsc(src) 0 · ESLint(src) clean · unit risk 43/43 + backup 66/66 + relapse-semantics 54/54 · test-spiritual-gate 10/18 — 8 failures PROVEN pre-existing (reverted the 3 edits temporarily → identical 10/18 → restored edits and re-verified) — intervention-selection logic, outside knowledge content scope, classified ENVIRONMENT/PRE-EXISTING FAILURE for a later product decision · production build pass · E2E 37/37 (incl. knowledge totals/categories/search/gating, daily dose from 148-pool, zero page errors, 11 screens × 320/360/390 no overflow) · NEW scripts/evidence-sweep.sh 29/29 (3 edited cards × 3 widths: new wording renders, old wording absent, no horizontal overflow, zero page errors; 9 screenshots in tool-results/evidence/) · VLM visual QA 9/9 (scripts/visual-qa-evidence.ts; RTL, no clipping, natural Arabic).
- Final self-review: no claim says more than evidence allows; scope = exactly the 16 claims; no stylistic rewording; no diagnosis/treatment promises; 148 cards structurally bit-identical except the 3 intended fields; all sources real with raw search evidence preserved; the only unsupported number removed rather than replaced.

Stage Summary:
- Evidence verification closed: 16/16 deferred claims reviewed with live web research — 13 kept verbatim (evidence supports current hedging, incl. the phone mere-presence claim now anchored to Carter 2016 JAMA Pediatrics), 3 minimally corrected (2 certainty softenings + 1 number removal). Zero cards added/deleted/merged; IDs/categories/tags/stages/spiritual gates/schema/order untouched; locked Arabic language untouched except the 3 evidence-forced edits. All QA gates green (15/15, tsc 0, ESLint clean, 43/43+66/66+54/54, build, 37/37, 29/29, VLM 9/9); the only failing gate (spiritual-gate 10/18) proven pre-existing and documented as environment, not content. Deliverables: phase3-knowledge-148-evidence-verified-final.md (new source of truth), phase3-knowledge-evidence-final.md, phase3-knowledge-evidence-sources.md, phase3-knowledge-evidence-change-map.md, phase3-knowledge-evidence-qa.md.

---
Task ID: 16
Agent: Super Z (main agent)
Task: FINAL NATIVE ARABIC EDITORIAL PASS (post-evidence round) — linguistic-only review of ALL 148 knowledge cards starting from phase3-knowledge-148-evidence-verified-final.md; NO evidence review, NO web search, NO science changes; preserve the 3 locked evidence changes; 3 deliverable docs + full regression.

Work Log:
- Read the full source of truth (148 cards, 2451 lines) + verified code↔doc sync programmatically (148/148 text fields identical; ID sets equal; doc order = category-grouped presentation, code order = core/wellbeing/recovery/new — both untouched by this round). Evidence-lock checks green (ur-wave «غالبًا خلال دقائق أو أقل من ساعة», en-friction «ثوانٍ قليلة», em-labeling «كثيرًا ما تخفض شدته»; old wording absent).
- Read all 148 cards card-by-card as a native Arabic reader (not a before/after comparator), judging every title and sentence with the 6 naturalness questions; baselines saved (scripts/task16-cards-baseline.json, task16-structure-baseline.json).
- Automated pattern scan (§20) on the source doc: all explicitly flagged phrases already at 0; found 93 semicolons (38-unique «؛ و» redundant-juncture family after fixing false positives from the header), 3 «معادلة» calques, 2 «هندسي» calques, plus individually flagged unnatural lines (تُبحر بأشرعة، يعرف الباقي، هذا هو السؤال، تقدم يُحسب، لا عندها، يعبر، أنت أول أصدقائك، عامله، أم الأبواب adjudicated-keep, etc.).
- Applied 48 card-scoped asserted edits (scripts/task16-apply-edits.py) to both the 4 TS files and the doc copy: content fixes (معادلة/الهندسة family, personification riddles, clipped constructions, translationese word order, title/body mismatches in sp-tawbah & va-day-design, pronoun calques) + 24 pure «؛ و»→«، و» punctuation unifications (each adjudicated; en-friction & lt-pressure deliberately kept and documented). Fixed a parser bug (regex match starts at '{' so rfind excluded it) and the doc-scoping bug (header mentions ur-wave as `cid`).
- §19 self-review: re-read all edited cards in final form with tests A–F → found 2 missed «هندسي» instances (rl-cut-point «هدفًا هندسيًّا», va-dissonance «وُجّه هندسيًّا») → applied 2 more edits (total 50 edits on 41 cards; 107 unchanged with explicit verdicts). Final tally: 24 pure punctuation + 26 language edits.
- Post-edit scans: zero flagged phrases; معادلة/هندسي = 0/0; «؛ و» = 2 (documented keeps); all 12 remaining «…» adjudicated (7 are «إذا… إذن» templates); variety checks pass (no template voice: top remember-opener «لا» 13/148; zero cross-card duplicate sentences ≥30 chars).
- Integrity validation (scripts/task16-validate.py): 148 cards/unique IDs; structure fingerprint (category/tags/stage) identical for all 148; text deltas = exactly the 50 intended edits; evidence locks verbatim; en-friction completely untouched; code↔doc sync 148/148; doc order preserved vs source doc.
- Regression: validate-knowledge 15/15 · tsc(src) 0 errors · ESLint(src) clean · unit risk 43/43 + backup 66/66 + relapse-semantics 54/54 · spiritual-gate 10/18 (pre-existing, proven: the test imports intervention-engine/interventions only, never knowledge) · production build pass · E2E 37/37 (knowledge totals/search/gating/daily-dose/zero page errors + 11 screens × 320/360/390 no overflow) · NEW scripts/task16-editorial-sweep.sh 85/85 (9 edited cards × 3 widths: new wording renders, old wording gone, no overflow; 1 click-flake on an off-screen button manually re-verified = NEW-RENDERED; 14 screenshots) · VLM visual QA (scripts/visual-qa-task16.ts) 14/14 (RTL, no clipping, natural Arabic).
- Deliverables regenerated: download/phase3-knowledge-148-native-arabic-final.md (new header documenting this round on top of the preserved evidence-round record; structural check: 148 IDs, 592 content labels, 5 gates, 1 deep, 20 categories summing 148, 3 evidence locks verbatim), phase3-knowledge-native-final-editorial-map.md (148/148: 41 UPDATED with before/after/type/reason + 107 UNCHANGED + documented intentional keeps), phase3-knowledge-native-final-qa.md (counts, families, examples, integrity, language QA, scans, full regression incl. OUT OF SCOPE note); download/README.md updated (this file is now the card-copy source of truth).

Stage Summary:
- The post-evidence native editorial round is closed: 148/148 cards read and judged, 41 minimally edited (50 edits: 24 punctuation unifications + 26 language fixes incl. the معادلة/هندسة calque family, artificial personifications, clipped grammar, 2 title alignments), 107 confirmed already-native and untouched. Zero structural/scientific changes: IDs/categories/tags/stages/gates/schema/order/numbers/certainty/recommendations bit-identical; the 3 evidence-locked changes preserved verbatim; en-friction untouched entirely. All QA gates green (15/15, tsc 0, ESLint clean, 43/43+66/66+54/54, build, E2E 37/37, sweep 85/85, VLM 14/14, zero overflow); spiritual-gate 10/18 remains the documented pre-existing environment failure. New card-copy source of truth: phase3-knowledge-148-native-arabic-final.md.
